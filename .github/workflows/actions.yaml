name: React Testing

on:
  push:
    branches: 
      - 'main'
      - 'development'
jobs:
  build:
    name: React Unit Test and Integration Test
    runs-on: ubuntu-latest
    timeout-minutes: 2
    strategy:
      matrix:
        node-version: [16.x]

    steps:
    - name: 👋🏻 Setup Repository
      uses: actions/checkout@v3

    - name: 📦 Clean & Install Dependencies
      run: npm ci

    - name: ⚡️ Run Tests
      run: npm run test